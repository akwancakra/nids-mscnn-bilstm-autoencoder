paths:
  # Updated to point to the new Drive location for Colab usage
  train_data: "/content/drive/MyDrive/nids-data/artifacts/data/research/s2_hybrid_zero_seed42_base/shards/cic/train"
  test_data: "/content/drive/MyDrive/nids-data/artifacts/data/research/s2_hybrid_zero_seed42_base/shards/cse/test"
  output_dir: "d:/Codingan/python/nids-projects/IntrusionDetectionSystem/output"
  model_save_path: "d:/Codingan/python/nids-projects/IntrusionDetectionSystem/models/sota_cnn_lstm.h5"
  # Scaler is likely in the base folder of the processed data
  scaler_path: "/content/drive/MyDrive/nids-data/artifacts/data/research/s2_hybrid_zero_seed42_base/scaler.pkl"

model:
  sequence_length: 10 # Fixed by the dataset
  n_features: 31      # Fixed by the dataset
  encoding_dim: 16    # Adjusted for smaller input
  learning_rate: 0.0005

training:
  batch_size: 256     # Larger batch size for NPZ
  epochs: 50
  validation_split: 0.1 # We can use separate val shards actually
  patience: 5

thresholding:
  method: "percentile" 
  percentile: 99.9 # High percentile for robustness
