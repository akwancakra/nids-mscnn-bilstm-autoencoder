paths:
  train_data: "d:/Codingan/python/nids-projects/nids-cnn-lstm-autoencoder/data/research/rs10_full_feature_filter_corr090/processed/shards/cic/train"
  test_data: "d:/Codingan/python/nids-projects/nids-cnn-lstm-autoencoder/data/research/rs10_full_feature_filter_corr090/processed/shards/cse/test"
  output_dir: "d:/Codingan/python/nids-projects/IntrusionDetectionSystem/output"
  model_save_path: "d:/Codingan/python/nids-projects/IntrusionDetectionSystem/models/sota_cnn_lstm.h5"
  # Scaler is already applied in the NPZ data, but we might want to save/load it if we do inference on new data
  scaler_path: "d:/Codingan/python/nids-projects/nids-cnn-lstm-autoencoder/data/research/rs10_full_feature_filter_corr090/processed/scaler.pkl"

model:
  sequence_length: 10 # Fixed by the dataset
  n_features: 31      # Fixed by the dataset
  encoding_dim: 16    # Adjusted for smaller input
  learning_rate: 0.0005

training:
  batch_size: 256     # Larger batch size for NPZ
  epochs: 50
  validation_split: 0.1 # We can use separate val shards actually
  patience: 5

thresholding:
  method: "percentile" 
  percentile: 99.9 # High percentile for robustness
